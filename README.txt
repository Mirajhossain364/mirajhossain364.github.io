To update your profile picture on the portfolio website:

1. Drop your image file into this `public` folder.
2. Ensure the image is named exactly: `profile.jpg` (or adapt the extension if it is a png, you will need to change it in App.jsx).
3. The website will automatically detect it and display it in the Hero section.
